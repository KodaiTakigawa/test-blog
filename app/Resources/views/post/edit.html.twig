<html>
    <body>
        <form action="{{ path('update', {'id': post.id }) }}" method="post">
            <input type="hidden" name="_method" value="put">
            <input name="appbundle_post[title]" type="text" value="{{post.title}}">
            <input name="appbundle_post[content]" type="text" value="{{post.content}}">
            {{ form_widget(form._token) }}
            <input type="submit">
        </form>
    </body>
</html>

{# {{ form(form) }} #}